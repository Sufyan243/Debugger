# Production environment — set these in your CI/CD secrets or hosting dashboard.
# VITE_ variables are inlined at build time; never put secrets here.

# Required: full URL of the deployed backend (no trailing slash)
# Example: https://api.yourdomain.com
VITE_API_BASE_URL=https://terradebugger.me

VITE_APP_URL=https://REPLACE_WITH_FRONTEND_URL
